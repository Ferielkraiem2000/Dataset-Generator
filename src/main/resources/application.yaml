spring:
  datasource:
    url: jdbc:postgresql://localhost:5500/datasetgeneratortool
    username: postgres
    password: feriel123**
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update