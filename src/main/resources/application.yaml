spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/datasetgenerator
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update